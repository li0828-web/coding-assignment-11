# Docker Assignment 11
# Docker Assignment: React App Setup

## Overview
This repository contains a Docker setup for a React application that displays a website with an `<h1>` tag containing the text "Codin 1". The application runs on `localhost:7775`.

## Prerequisites
- Docker installed on your system
- Basic command line knowledge

## Getting Started

### 1. Clone the Repository
```bash
git clone <your-repository-url>
cd <repository-folder>